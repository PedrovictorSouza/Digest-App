{
  "version": 2,
  "buildCommand": "cd apps/react-vite && yarn install && yarn build",
  "outputDirectory": "apps/react-vite/dist",
  "installCommand": "cd apps/react-vite && yarn install",
  "framework": null,
  "functions": {},
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
